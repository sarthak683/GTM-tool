Review the current frontend change for UI quality.

Use `http://localhost:8080`.

Check:
- desktop layout
- mobile layout
- text overflow
- z-index and popover clipping
- hover/focus states
- drawer/modal reachability
- button loading/disabled states
- console/runtime errors where browser tooling allows

If problems are found, fix them and rebuild frontend with:

`make rebuild-frontend`

