Run a local smoke test for Beacon GTM.

Steps:
1. Read `AGENTS.md`.
2. Run `make ps`.
3. Run `make smoke`.
4. If UI changed, open `http://localhost:8080` and verify the changed flow.
5. Report pass/fail, changed files, and exact errors if any.

