Run mobile QA for the changed workflow.

Use `http://localhost:8080`.

Check:
- viewport width around 390px
- drawers and modals take usable width/height
- primary actions are reachable without awkward scrolling
- sticky footers do not cover form fields
- dropdowns/popovers are above content and not clipped
- table/card text does not overlap

Fix issues found, rebuild frontend, then retest.

