# Beacon ↔ Recotap — Integration Proposal

**From:** Beacon (GTM / CRM platform)  **For:** Recotap
**Purpose:** Confirm scope and get your sign-off for a bi-directional API integration.

---

## 1. Summary

Beacon is a GTM/CRM platform. We'd like to connect to Recotap so that:

- **Beacon → Recotap:** we send our **accounts, deals, and call/email activities** so Recotap has live CRM context.
- **Recotap → Beacon:** we read back each account's **score, buying-journey stage, and intent sub-scores** to surface inside Beacon.

Recotap remains the source of truth for intent/scoring; Beacon remains the source of truth for CRM data. The integration is **additive and read-only on your scores** — we never modify Recotap's scoring logic or data.

---

## 2. Data flow

| Direction | Data | Source of truth |
|---|---|---|
| Beacon → Recotap | Accounts (name + domain), Deals, Call/Email activities | Beacon |
| Recotap → Beacon | Account score, journey stage, intent sub-scores | Recotap |

**Account match key:** `domain` (lowercased, `www`-stripped). We also send our own `externalId` for reference.

---

## 3. API usage

| | |
|---|---|
| Auth | `X-Api-Key` header (per-workspace key you issue us) |
| Transport | HTTPS, JSON REST |
| Push endpoints | `POST /accounts`, `POST /accounts/map-external-ids`, `POST /deals`, `POST /sales-activities`, `POST /deal-stages` |
| Pull endpoint | `GET /accounts` (delta + pagination) |
| Pagination | keyset: `cursor`/`limit` → `nextCursor`, `hasNextPage`, `syncTimestamp` |
| Delta sync | `lastSync=<ISO-8601>` returns modified-since records only |
| Batch sizes | ≤ 100 accounts · ≤ 100 deals · ≤ 50 activities per request |
| Webhooks | Not required — we poll on a schedule |

We will only call the endpoints listed above.

### Example calls

*Illustrative payloads — exact field names per your API spec; happy to align.* Auth header on every request:

```
X-Api-Key: <workspace_api_key>
Content-Type: application/json
```

**Push accounts**
```json
POST /api/v1/accounts
{
  "accounts": [
    {
      "externalId": "bcn_8f2a91",
      "name": "Haily HR",
      "domain": "hailyhr.com",
      "customFields": {
        "icp_score": 82,
        "account_thesis": "Mid-market HR SaaS, expanding PS team",
        "beacon_angle": "Implementation automation"
      }
    }
  ]
}
```

**Push a deal (associated to an account by domain)**
```json
POST /api/v1/deals
{
  "deals": [
    {
      "externalDealId": "bcn_deal_4471",
      "amount": 48000,
      "stageLabel": "Discovery",
      "stageId": "stg_discovery",
      "closedDate": "2026-08-15",
      "ownerEmail": "rep@beacon.li",
      "associatedAccounts": [{ "domain": "hailyhr.com" }]
    }
  ]
}
```

**Push a call/email activity**
```json
POST /api/v1/sales-activities
{
  "activities": [
    {
      "accountDomain": "hailyhr.com",
      "activityType": "call",
      "occurredAt": "2026-06-01T14:32:00Z",
      "durationMinutes": 12,
      "ownerEmail": "rep@beacon.li"
    }
  ]
}
```

**Pull scores (delta + pagination)**
```json
GET /api/v1/accounts?lastSync=2026-06-01T00:00:00Z&limit=100&cursor=<nextCursor>

200 OK
{
  "results": [
    {
      "domain": "hailyhr.com",
      "externalId": "bcn_8f2a91",
      "accountScore": 76,
      "journeyStage": "Consideration",
      "intentScores": { "linkedin": 71, "g2": 64, "bombora": 80, "website": 58 }
    }
  ],
  "nextCursor": "eyJpZCI6IDEwMjN9",
  "hasNextPage": true,
  "syncTimestamp": "2026-06-02T09:40:00Z"
}
```

> A Word (`.docx`) version of this proposal is generated by `docs/build_recotap_docx.py` → `docs/Recotap_Integration_Proposal.docx`.

---

## 4. Sync cadence & expected load

Poll-based, delta-only:

- **Push** ~every 5 min — only records changed since last sync, batched within your limits.
- **Pull** ~every 15 min — `GET /accounts?lastSync=…`, paginated.

Steady-state volume is low (only changed records per cycle). The one-time initial backfill is chunked within the batch limits above. We will honor any rate limits you specify.

---

## 5. What we send

- **Accounts:** company name, domain, and a small set of CRM fields (e.g. ICP score, account notes) as `customFields`.
- **Deals:** amount, stage, close date, owner email, associated account domain.
- **Activities:** **calls and emails only** — type, timestamp, duration. We will not send meeting transcripts, notes, or other internal activity types unless you support and want them.

## 6. What we read

Per account: **score, journey stage, intent sub-scores** — displayed inside Beacon. Strictly read-only.

---

## 7. Security & data handling

- API key stored **encrypted per workspace**; never exposed in any client/browser.
- All traffic over **HTTPS**.
- We send only **B2B account and activity metadata** — no consumer PII.
- Integration is **feature-flagged and off by default**, enabled only after both sides validate in a test workspace.

---

## 8. What we need from Recotap

1. A **sandbox / test API key** + workspace to validate against.
2. Confirm the **HTTPS base URL** and current endpoint paths.
3. **Rate limits** and any concurrency constraints.
4. Confirm **supported activity types** (we plan to send `call` + `email`).
5. How to **register deal stages** — we'll push our stage taxonomy via `POST /deal-stages` and reuse the IDs you return.
6. Typical **latency** from an account push to its score being populated.
7. Any **pricing / plan** implications for API access.

---

## 9. Rollout

1. Validate in a **test workspace** with the sandbox key.
2. **One-time backfill:** accounts → dedup map (`map-external-ids`) → deals → call/email activities → first full pull.
3. Enable **incremental delta sync** (5-min push / 15-min pull) behind the flag.

---

*Questions or changes to scope are welcome — we can adjust endpoints, cadence, or field mapping to fit your platform's constraints.*
